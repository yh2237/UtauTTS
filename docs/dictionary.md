# 辞書設定

GUIの「設定」→「辞書設定...」から表記と読みの対応を登録します。

## 登録方法

1. 「表記」に文章中で置き換えたい文字列を入力します。
2. 「読み」に読み上げたいひらがなまたはカタカナを入力します。
3. 「適用」で反映します。「OK」なら反映して画面を閉じます。

### 例

| 表記 | 読み |
| --- | --- |
| `UtauTTS` | `うたうてぃーてぃーえす` |

辞書は`config.ini`へ保存され、次回起動時も使われます。反映すると入力済みの文章も現在の辞書で再解析されます。「キャンセル」では今回の変更を破棄します。
