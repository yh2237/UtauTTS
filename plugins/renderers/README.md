# Renderer packages

Built-in renderers are registered in `internal/plugin/builtin.go`; no manifest is required.
Install external v2 ZIP packages through File > Renderer plugins. Legacy v1 manifests
can still be placed here manually. See `docs/plugins.md` for the v2 format.
